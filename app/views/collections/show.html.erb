<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @collection.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @collection.description %>
</p>

<p>
  <strong>User:</strong>
  <%= @collection.user_id %>
</p>

<%= link_to 'Edit', edit_user_collection_path(id: @collection.id, user_id: @collection.user_id) %> |
<%= link_to 'Back', user_collections_path(user_id: @collection.user_id) %>
