<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @tag.title %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @tag.tag_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @tag.user_id %>
</p>

<%= link_to 'Edit', edit_user_tag_path(id: @tag.id, user_id: @tag.user_id) %> |
<%= link_to 'Back', user_tags_path(user_id: @tag.user_id) %>
